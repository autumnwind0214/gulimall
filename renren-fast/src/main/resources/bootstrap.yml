spring:
  application:
    name: renren-fast
  cloud:
    nacos:
      config:
        namespace: gulimall
        server-addr: 127.0.0.1:8848
        file-extension: yml
        ext-config:
          - data-id: common.yml
            group: common
            refresh: true
server:
  port: 7008