spring:
  cloud:
    nacos:
      config:
        namespace: gulimall
        server-addr: 127.0.0.1:8848
        file-extension: yml
        ext-config:
          - data-id: ware-basic.yml
            group: gulimall-ware
            refresh: true
          - data-id: common.yml
            group: common
            refresh: true